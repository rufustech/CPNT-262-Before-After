//Find Index: You can find the position of a specific word or character within a string using a dedicated method.

let word = "together";
console.log(word);

// find a word using the spice method - geting the work Get
word = word.slice(2, 5);
console.log(word);
